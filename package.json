{
    "name": "mongo-app",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "node index",
        "dev": "nodemon --exec 'node index'",
        "test": "nodemon --exec 'mocha --recursive -R' min"
    },
    "dependencies": {
        "express": "^4.17.1",
        "mocha": "^9.1.3",
        "mongoose": "^6.0.12",
        "nodemon": "^2.0.15",
        "supertest": "^6.1.6"
    }
}
